# 1. 核心：必须忽略的巨大文件夹 (解决构建超时的关键)
node_modules
.git

# 2. 构建输出目录 (Vite 默认输出到 dist，避免将本地旧构建覆盖进去)
dist
build

# 3. 本地环境日志和调试文件
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 4. 系统和 IDE 生成的垃圾文件
.DS_Store
.idea
.vscode
*.swp

# 5. 可选：不需要打包进镜像的文档或配置
README.md
.gitignore
.dockerignore
Dockerfile
